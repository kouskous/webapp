import {Route} from '@angular/router';
import {CreateCompanyComponent} from './create-company.component';

export const createCompanyRoutes: Route[] = [
    {
        path: '',
        component: CreateCompanyComponent
    }
];
